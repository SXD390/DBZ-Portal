/**
 * Fill these placeholders after you deploy backend:
 * Example: https://abc123.execute-api.ap-south-1.amazonaws.com/prod/
 * MUST end with a trailing slash.
 */
export const API_BASE = "https://REPLACE_ME.execute-api.ap-south-1.amazonaws.com/prod/"

/**
 * If you ever want to show the CloudFront media domain in the UI (optional).
 * Example: dxxx.cloudfront.net
 */
export const MEDIA_CF_DOMAIN = "dXXXX.cloudfront.net" // not required for the app to run


